# Firebase Hosting only serves the static SPA — it has no knowledge of /api/*,
# so without this the axios client falls back to a same-origin relative
# baseURL and every API call silently gets Firebase's SPA index.html back
# instead of a real response. Point it at the real gateway.
VITE_API_BASE_URL=https://portfolio-api.manhnpc.com/api
